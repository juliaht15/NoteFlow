{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "exclude": [
    "node_modules",
    "noteflow-api" // <--- Añade esto para que la app móvil no intente compilar el backend
  ]
}